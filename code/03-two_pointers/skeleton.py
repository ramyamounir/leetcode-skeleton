
def two_pointer(arr):

  # Initialize two pointers
  left = 0
  right = len(arr) - 1

  # Initialize variables to store the results
  result = []

  # Loop until the pointers meet
  while left < right:
    # Do something with the elements at the pointers
    # and update the result as needed

    # Move the pointers
    left += 1
    right -= 1

  return result
